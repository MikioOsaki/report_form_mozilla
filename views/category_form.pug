extends layout

block content
  h1 #{title}

  form(method='POST' action='')
    div.form-group
      label(for='name') Name der Kategorie:
      input#name.form-control(type='text', placeholder='Wasser, Müll etc.' name='name' value=(undefined===category ? '' : category.name))
    button.btn.btn-primary(type='submit') Erstellen

  if errors 
    ul
      for error in errors
        li!= error.msg